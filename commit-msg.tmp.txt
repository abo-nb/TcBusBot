--dryrun：掃組件確認每個模組都在 BotModules 清單裡

補一道防線，避免「寫好模組卻忘了註冊」再發生一次（症狀是線上不存在那組
指令／按鈕，而且不會報錯）。

寫的時候這道防線自己先抓到一件事：7 個模組只掃到 3 個 —— 因為大部分模組
繼承 BusModuleBase（為了 UpdateAsync），只看**直接**基底類別會漏掉它們。
改成往上走完整條繼承鏈之後，7 個全部掃到並確認都在清單裡。
