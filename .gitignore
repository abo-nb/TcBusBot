# ── 機密：絕對不要提交 ──────────────────────────────────
# Discord Bot Token 等同密碼；TDX 金鑰也一樣。
.env
.env.*
!.env.example

# ── TDX 靜態資料快取（可重新抓，不需要進版控）────────────
cache/

# ── 訂閱組資料庫（使用者資料，不進版控）──────────────────
tcbus.db
tcbus.db-wal
tcbus.db-shm
*.db
*.db-wal
*.db-shm

# ── .NET 建置產物 ──────────────────────────────────────
bin/
obj/
*.user
*.suo

# ── 編輯器 ─────────────────────────────────────────────
.vs/
.vscode/
.idea/

# Android APK 產物（build-apk.ps1 的輸出）
dist/
